'use strict';

var BigNumber = require('big.js');

module.exports = {
  normalizeDecimal: normalizeDecimal
};

function normalizeDecimal(decimal) {
  if (decimal.indexOf('e') > -1) {
    return new BigNumber(decimal).toFixed();
  }
  return decimal;
}
